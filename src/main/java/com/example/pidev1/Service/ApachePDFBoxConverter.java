package com.example.pidev1.Service;


import org.springframework.stereotype.Service;

import java.io.File;
import java.io.IOException;
/*
@Service
    public class ApachePDFBoxConverter implements PDFToTextConverter {
        public String convertPDFToText(String filePath) throws IOException {
            // Ouvre le document PDF
            PDDocument document = PDDocument.load(new File(filePath));

            // Instancie un objet PDFTextStripper pour extraire le texte
            PDFTextStripper pdfStripper = new PDFTextStripper();

            // Obtient le texte extrait du document PDF
            String text = pdfStripper.getText(document);

            // Ferme le document PDF
            document.close();

            return text;
        }
    }*/


